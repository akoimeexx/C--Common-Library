CREATE TABLE [dbo].[Hashes] (
	[fk] NVARCHAR(72) NOT NULL 
        UNIQUE NONCLUSTERED ([fk] ASC) PRIMARY KEY NONCLUSTERED ([fk] ASC), 
    [hash] NVARCHAR(MAX) NULL, 
    [salt] NVARCHAR(MAX) NULL, 

	CONSTRAINT [UserLink] FOREIGN KEY ([fk]) REFERENCES [dbo].[Users] ([pk])
		ON DELETE CASCADE ON UPDATE CASCADE
);

INSERT INTO [dbo].[Hashes] (
    [fk], [hash], [salt]
) VALUES (
    N'01234567-8901-2345-6789-012345678901', NULL, NULL
);
